server_addr = '127.0.0.1:7373'
max_workers = 4
review_context_count = 3
reuse_review_session_lookback = 0
{{- $enpass := findOneExecutable "enpass-cli" "enpasscli" -}}
{{- $enpassVault := .enpassVault -}}
{{- $roborevUsername := output "sh" "-c" (printf "eval \"$(%s -vault %s env -field %s ROBOREV_USERNAME=%s)\" && printf '%%s' \"$ROBOREV_USERNAME\"" $enpass (shellQuote $enpassVault) (shellQuote "username") (shellQuote "Roborev Sync")) | trim -}}
{{- $roborevPassword := output $enpass "-vault" $enpassVault "pass" "Roborev Sync" | trim -}}
# Extra review instructions added to prompts globally.
review_guidelines = ''
# Default agent when no workflow-specific agent is set.
default_agent = 'claude'
default_backup_agent = 'opencode'
default_backup_model = 'opencode-go/kimi-k3'
default_model = 'claude-opus-4-8'
refine_model = 'sonnet'
classify_model = 'haiku'
# Default reasoning level for reviews: fast, standard, medium, thorough, or maximum.
review_reasoning = 'medium'
# Default reasoning level for refine: fast, standard, medium, thorough, or maximum.
refine_reasoning = 'standard'
# Default reasoning level for fix: fast, standard, medium, thorough, or maximum.
fix_agent = 'opencode'
fix_model = 'opencode-go/deepseek-v4-flash'
fix_reasoning = 'standard'

job_timeout_minutes = 30
# Post-commit hook request timeout in seconds. 0 or negative uses the platform default (3 on most systems, 30 on Windows where git subprocess spawns are slow).
hook_timeout_seconds = 0
# Maximum daemon-wide cooldown after an agent quota error, as a Go duration such as 30m.
agent_quota_cooldown = '30m0s'

{{- if false }}
review_agent = ''
review_agent_fast = ''
review_agent_standard = ''
review_agent_medium = ''
review_agent_thorough = ''
review_agent_maximum = ''
refine_agent = ''
refine_agent_fast = ''
refine_agent_standard = ''
refine_agent_medium = ''
refine_agent_thorough = ''
refine_agent_maximum = ''
review_model = ''
review_model_fast = ''
review_model_standard = ''
review_model_medium = ''
review_model_thorough = ''
review_model_maximum = ''
refine_model = ''
refine_model_fast = ''
refine_model_standard = ''
refine_model_medium = ''
refine_model_thorough = ''
refine_model_maximum = ''
fix_agent = ''
fix_agent_fast = ''
fix_agent_standard = ''
fix_agent_medium = ''
fix_agent_thorough = ''
fix_agent_maximum = ''
fix_model_fast = ''
fix_model_standard = ''
fix_model_medium = ''
fix_model_thorough = ''
fix_model_maximum = ''
security_agent = ''
security_agent_fast = ''
security_agent_standard = ''
security_agent_medium = ''
security_agent_thorough = ''
security_agent_maximum = ''
security_model = ''
security_model_fast = ''
security_model_standard = ''
security_model_medium = ''
security_model_thorough = ''
security_model_maximum = ''
design_agent = ''
design_agent_fast = ''
design_agent_standard = ''
design_agent_medium = ''
design_agent_thorough = ''
design_agent_maximum = ''
design_model = ''
design_model_fast = ''
design_model_standard = ''
design_model_medium = ''
design_model_thorough = ''
design_model_maximum = ''
# Agent for the design-review routing classifier. Must implement SchemaAgent capability.
classify_agent = ''
# Model for the classifier agent. Empty = agent default.
classify_model = ''
# Reasoning level for the classifier: fast, standard, medium, thorough, or maximum.
classify_reasoning = ''
# Fallback classifier agent on quota exhaustion / failure.
classify_backup_agent = ''
# Fallback classifier model.
classify_backup_model = ''
review_backup_agent = ''
refine_backup_agent = ''
fix_backup_agent = ''
security_backup_agent = ''
design_backup_agent = ''
review_backup_model = ''
refine_backup_model = ''
fix_backup_model = ''
security_backup_model = ''
design_backup_model = ''
# Minimum severity for reviews: critical, high, medium, or low. Empty disables filtering.
review_min_severity = ''
# Minimum severity for refine: critical, high, medium, or low. Empty disables filtering.
refine_min_severity = ''
# Minimum severity for fix: critical, high, medium, or low. Empty disables filtering.
fix_min_severity = ''
# Author for roborev-owned fix commits, formatted as Name <email>.
fix_commit_author = ''
# Co-authored-by trailers for roborev-owned fix commits, each formatted as Name <email>.
fix_commit_co_authored_by = []
# Filenames or glob patterns to exclude from review diffs globally.
exclude_patterns = []
default_max_prompt_size = 0
# Automatically close reviews that pass with no findings.
auto_close_passing_reviews = false
# Hide closed reviews by default in the TUI queue.
hide_closed_by_default = false
hide_addressed_by_default = false
# Automatically filter the TUI queue to the current repo.
auto_filter_repo = false
# Automatically filter the TUI queue to the current branch.
auto_filter_branch = false
# Show auto-design-review classifier rows (and skipped design rows) in the TUI queue. Off by default to reduce noise.
show_classify_jobs = false
# Enable mouse support in the TUI.
mouse_enabled = true
tab_width = 0
# Queue columns to hide in the TUI.
hidden_columns = []
# Show column borders in the TUI queue.
column_borders = false
# Custom queue column order in the TUI.
column_order = []
# Custom Tasks column order in the TUI.
task_column_order = []
column_config_version = 0

{{- end }}

[sync]
enabled = true
postgres_url = 'postgres://{{ $roborevUsername }}:{{ $roborevPassword }}@app-cluster-18-rw.postgres.k8s.icb4dc0.de:5432/roborev'
interval = '5m'
{{- $computerName := "" }}
{{- if eq .chezmoi.os "darwin" }}
{{- $computerName := output "scutil" "--get" "ComputerName" | trim }}
{{- else if eq .chezmoi.os "linux" }}
{{- $computerName := output "hostname" | trim }}
{{- end }}
machine_name = '{{ $computerName }}'
connect_timeout = '30s'

[cost]
# HTTP usage endpoint template for cost/token lookup. Use {session_id}; set empty to use agentsview CLI lookup only.
endpoint = ''
# Timeout for HTTP usage endpoint lookups.
timeout = '10s'

[agent]

[auto_design_review]
# Enable the automatic design review decider. Off by default.
enabled = false
# Enable the automatic design review decider only for post-commit hook reviews. Off by default.
hook_enabled = false
# Diffs below this line count skip design review automatically.
min_diff_lines = 0
# Diffs at or above this line count trigger a design review automatically.
large_diff_lines = 0
# Commits touching at least this many files trigger a design review automatically.
large_file_count = 0
# Doublestar globs; any changed file matching triggers a design review.
trigger_paths = []
# Doublestar globs; a commit whose changed files all match is skipped.
skip_paths = []
# Regexes over the commit subject; a match triggers a design review.
trigger_message_patterns = []
# Regexes over the commit subject; a match skips the design review.
skip_message_patterns = []
# Per-classify-job timeout.
classifier_timeout_seconds = 0
# Cap on classifier prompt size in bytes.
classifier_max_prompt_size = 0

[review]
default_panel = ''
hook_review_panel = ''

[agent_hook]
# Stop hook threshold; 0 disables Stop-based prompting.
turn_threshold = 5
# PostToolUse commit threshold; 0 disables commit-based prompting.
commit_threshold = 0
# Open failed roborev review threshold; 0 disables review-based prompting.
failed_review_threshold = 4
# Instruction emitted when the agent hook decides a review/fix pass is needed.
instruction = 'Invoke the $roborev-fix skill now.'

[kata_context]
# Kata task context in review prompts: off | current | open (default off).
mode = 'off'
# Max bytes of kata context to include (default 50000).
max_chars = 50000

[advanced]
# Enable the advanced Tasks workflow in the TUI.
tasks_enabled = false
